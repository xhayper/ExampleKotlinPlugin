# ExampleKotlinPlugin

An example Spigot plugin written in Kotlin.

## Building

### Windows

```batch
./gradlew.bat build
```

### Linux / macOS

```batch
./gradlew build
```

and your plugin will appear in `build/libs/`